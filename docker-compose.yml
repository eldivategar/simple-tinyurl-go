version: '3.8'

services:
  app:
    build: .
    ports:
      - "7860:7860"
    environment:
      - REDIS_ADDR=redis:6379
      - SERVER_URL=http://localhost:7860
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
